# The change under review

Measured against `origin/main`.

## Files changed

- `src/domain/order.ts` (modified)
- `src/infra/http.ts` (renamed), was `src/infra/fetch.ts`

## This repository's own guidance

These are the documents this repository asked to be reviewed against. Where they disagree with general practice, they win.

### `docs/conventions.md`

# Conventions

The domain layer takes a client.

## Documentation this change may have invalidated

- `docs/architecture.md` describes `src/domain/order.ts` and `src/infra/http.ts`

## Already reported by the deterministic analyzers

Do not repeat these. They are here so you can spend your attention on what they cannot see.

- `types/TS2322` at `src/domain/order.ts:12` — Type 'number' is not assignable to type 'string'. …

## The diff

```diff
diff --git a/src/domain/order.ts b/src/domain/order.ts
@@ -8,4 +8,6 @@ export class Order {
   private readonly id: string;
+  async total(): Promise<number> {
+    return 0;
+  }
 }
```
