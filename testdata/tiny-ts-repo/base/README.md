# tiny-ts-repo

A two-commit TypeScript repository used as a fixture. The second commit
introduces one architectural boundary violation, one floating promise and one
credential, so every analyzer has something true to find.

Do not "fix" the problems in `head/`: they are the point.
